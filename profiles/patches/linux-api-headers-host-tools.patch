--- a/gpkg/linux-api-headers/build.sh
+++ b/gpkg/linux-api-headers/build.sh
@@ -17,7 +17,14 @@ termux_step_make() {
 }
 
 termux_step_make_install() {
-	make -C "${TERMUX_PKG_SRCDIR}" INSTALL_HDR_PATH="${TERMUX__PREFIX__INCLUDE_DIR}" ARCH="${LINUX_ARCH}" headers_install
+	(
+		if [ "$TERMUX_ON_DEVICE_BUILD" = "false" ]; then
+			unset CFLAGS CXXFLAGS CC CXX AR RANLIB NM CXXFILT
+			export PATH="/usr/bin"
+		fi
+		make -C "${TERMUX_PKG_SRCDIR}" \
+			INSTALL_HDR_PATH="${TERMUX__PREFIX__INCLUDE_DIR}" ARCH="${LINUX_ARCH}" headers_install
+	)
 	rm -r "${TERMUX__PREFIX__INCLUDE_DIR}/drm"
 }
